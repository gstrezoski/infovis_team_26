// Import styles
// Only import the main style file here, import the rest in main.scss itself
import './assets/styles/sass/main.scss'

// Import JS classes
// Same logic, only reference files here, don't put actual code in this file, but defer to
// assets/scripts/main.js or a separate file
import '_scripts/main.js';
